#include <stdio.h>
/**
 * main - print number of args passed to it
 * @argc: arg number
 * @argv: arguments
 * Return: 0
 */
int main(int argc, char **argv)
{
void(argc);
printf("%d\n", argc - 1);
return (0);
}
