#include "main.h"
/**
 * _abs - compute the absolute value of int
 * Return: the absolute value of integer
 * @n: the integer not be computed
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
