#include "main.h"
/**
 * print_line - print line in terminal
 * @n: number of _ will drawn
 * Return: only void
 */
void print_line(int n)
{
int x, n;
for (x = 0; x <= n; x++)
{
if (n = 0)
{
_putchar('\n')
}
_putchar('_');
}
_putchar('\n');
}
